# 更新日志

## 3.9.11

- Boss 锁定 OSC 增加“仅输出玩家名”选项，可隐藏 `Boss 当前锁定：` 前缀，切换后立即重发当前目标。

## 3.9.10

- 关闭 Boss 锁定 OSC 输出时立即向 VRChat Chatbox 发送清空消息，文字不再等待自动超时。

## 3.9.9

- 新增 Ecliptica Boss 当前锁定 OSC 输出，可配置目标主机和端口，默认使用 VRChat Chatbox 的 `/chatbox/input`。
- OSC 文本使用 UTF-8 编码，兼容 `ಣಪರೀಕ್ಷೆ` 等包含特殊 Unicode 字符的玩家名称。

## 3.9.8

- Ecliptica Boss 伤害结算表新增“耗时”列，显示每个 Boss 阶段从开始到结算使用的时间。

## 3.9.7

- Boss 当前锁定改为解析 `ownership of <Boss> transferred to <玩家名>`，现在可显示 Boss 正在锁定的具体玩家。
- 解析 VRChat 本机认证名称，用于准确区分“正在追击你”和“追击其他玩家”；受伤日志不再覆盖明确的 Boss 所有权目标。

## 3.9.6

- 修正 Boss 当前锁定状态机，与原工具保持一致：Boss 战开始后显示“某玩家 / 仇恨中”，本机受击时显示“你 / 正在追击你”，锁定窗口结束后显示“其他玩家 / 追击其他玩家”。
- 状态过期现在只标记“状态可能已过时”，不再清空 Boss 锁定目标并永久显示“未确认”。

## 3.9.5

- HUD 缩放现在会同步调整文字大小、两列间距、行距和纵向位置，不再只放大背景。
- 配置预览与保存后的运行态 HUD 使用同一套等比缩放计算。

## 3.9.4

- 重写 HUD 布局配置模式，使用独立的单窗口预览，不再让运行态分层文字窗口参与拖拽和缩放。
- 使用 Win32 `SetWindowPos` 与 `GetWindowRect` 应用和保存预览矩形，绕过透明无边框窗口的 Tk 坐标缓存问题。
- 修复恢复默认后旧拖拽状态覆盖默认坐标的问题，保存时再一次性同步运行态背景层与文字层。

## 3.9.3

- 修复布局配置拖拽时文字层移动但背景层停留在原位的问题，两层现在共享同一目标矩形。
- 恢复默认现在会明确重置两个 HUD 的位置和尺寸，并在保存前同步前景与背景窗口。

## 3.9.2

- 将背景透明度滑块修正为真实透明度语义，支持 `0%` 不透明至 `100%` 完全透明。
- 自动迁移旧版 `hud_opacity` 配置，保持升级前的实际视觉效果。
- 完全透明时进入布局配置会临时显示预览底框，保存后恢复完全透明。

## 3.9.1

- 将 Ecliptica 伤害 HUD 与 Boss 锁定 HUD 的全部文字统一为纯白色 `#FFFFFF`。

## 3.9.0

- 将 Ecliptica HUD 文字层从 Tk 色键透明迁移到 Windows `UpdateLayeredWindow` 逐像素 Alpha 合成。
- 背景透明度与文字 Alpha 完全分离，文字保持完全不透明并使用灰度抗锯齿，不再需要描边或阴影。
- 优化预乘 BGRA 转换性能，并修复编辑缩放过程中前景与背景窗口短暂不同步的问题。

## 3.8.9

- 移除 Ecliptica HUD 文字的黑色描边和阴影，文字现在仅绘制原始颜色。

## 3.8.8

- 将 HUD 的八方向黑色描边改为单方向柔和阴影，降低视觉刺激。
- 伤害 HUD 改为标签与数值分栏显示，并降低状态强调色的饱和度。

## 3.8.7

- 为 Ecliptica HUD 文字增加高对比深色描边，改善低透明背景及复杂画面下的可读性。

## 3.8.6

- 修复 HUD 透明度会同时影响文字的问题，改为仅调整背景层透明度，文字层始终保持完全不透明。

## 3.8.5

- 增加 Ecliptica HUD 背景透明度滑块，支持 `20%` 至 `100%` 实时调整并保存。
- 增加恢复默认按钮，可恢复默认位置、尺寸、`90%` 背景透明度和两者共同显示。

## 3.8.4

- 修复启用 Ecliptica HUD 后未立即显示的问题，HUD 现在完全由用户手动开关控制。
- 增加 HUD 显示内容选择，可单独显示伤害数据、Boss 当前锁定或同时显示两者。

## 3.8.3

- 增加 Ecliptica HUD 布局配置预览，可分别拖动伤害框和 Boss 锁定框的位置。
- 两个 HUD 预览框支持拖动右下角控制点调整大小，并持久保存位置和尺寸。

## 3.8.2

- 将数据面板选择器移动到顶部工具栏最左侧，确保窗口宽度较小时仍可看到并切换。
- 删除任务地点手动选择功能，物品参考图统一使用通用位置映射。

## 3.8.1

- 增加面板显示模式选择，可使用自动跟随地图或手动固定显示 SlashCo、Ecliptica 面板。
- 手动固定面板时继续在后台识别和解析当前地图日志，Ecliptica 桌面 HUD 仍以实际地图为准。

## 3.8.0

- 集成 Ecliptica 日志分析，显示职业、关卡、Boss 阶段、分阶段伤害、DPS 和受伤来源。
- 根据 VRChat 当前地图自动切换 SlashCo 与 Ecliptica 数据面板，并支持从日志尾部恢复 Ecliptica 会话。
- 增加可手动开关的透明置顶桌面 HUD，分别显示 Ecliptica 战斗数据和 Boss 当前锁定状态。
- Boss 锁定根据本机近期受击日志推测，并明确区分“你”“其他玩家（推测）”和“未确认”。

## 3.7.0

- 增加赞助者名单覆盖模式切换，可在 `mitmdump` 与 `hosts + Caddy` 之间选择。
- 修复 mitmproxy CA 信任状态误判，避免自检通过但游戏实际 TLS 拦截失败。
- 优化 Clash/加速器兼容性，`hosts + Caddy` 模式仅为 Pastebin 添加代理绕过，不再关闭系统代理或 PAC。
- 增加模式切换提示，当前模式不生效时引导用户切换另一个模式尝试。

## 3.6.9

- 修正多人局燃油减免显示，固定按 1 人少 4 桶、2 人少 3 桶、3 人少 2 桶、4 人少 1 桶、5 人及以上不减免。
- 修复检测到 0 个锁门时锁门信息被隐藏的问题。

## 3.6.8

- 修复同一局内重复开始信号触发二次重置，导致玩家减免和锁门数量显示后消失的问题。

## 3.6.7

- 增加对局计时显示，开始对局后自动计时。
- 25 分钟内以浅绿色显示，超过 25 分钟后变红并提示超时。

## 3.6.6

- 优化赞助覆盖代理启动逻辑，系统代理改为同时写入 HTTP/HTTPS 分协议地址。
- 启动前清理本工具残留的 mitmdump 进程，并在固定端口不可用时自动切换随机端口。

## 3.6.5

- 统一运行时缓存、配置、图片和赞助覆盖数据到用户本地数据目录。
- 保持单 exe 所在目录干净，除自动更新临时备份 `.old` 外不再主动写入运行数据。

## 3.6.4

- 增加启动版本日志，便于验证自动更新后是否成功打开新版本。
- 用于验证 3.6.3 修复后的自动更新重启流程。

## 3.6.3

- 修复单文件程序自更新后新版本继承旧临时解包目录，导致启动失败的问题。

## 3.6.2

- 将燃油进度从发电机状态中拆出，改为全局 `8/当前桶数` 计数。
- 根据局内人数自动设置初始燃油减免；无法识别人数时默认从 `8/0` 开始。
- 电池状态继续按两个发电机分别显示。

## 3.6.1

- 优化启动日志恢复：只从最新日志尾部扫描当前对局，避免从第一个旧回合回放到最新回合。
- 拆分日志解析逻辑并增加单元测试，降低后续地图日志格式变化时的维护成本。
- 修复实时日志投递，启动恢复结束后继续正确识别新对局、物品刷新和位置变化。
- 修复新版地图发电机燃油和电池进度识别。
- 修复对局结束后的状态重置。
- 增加单 exe 自动更新框架，支持后台检查、镜像优先下载、进度显示和自替换流程。
- 优化更新 UI，无更新或检查失败时默认隐藏，减少对主界面的干扰。
